server:
  port: 8002
spring:
  application:
    name: consumer-user
eureka:
  instance:
    #注册服务的时候使用服务的IP地址
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka/